import React from "react";
import s from "../styles/components/custommodal.module.scss";

const CustomModal = ({ title, subtitle, phraze = "", path }) => {
	return <div className={s.modal}>
        
    </div>;
};

export default CustomModal;
